# Claude Code session data
.claude/
# Local environment overrides (may contain real secrets)
values-prod.yaml
values-staging.yaml
values-dev.yaml
# values-local.yaml is intentionally tracked: it contains only dev-safe defaults
# values.local.yaml is gitignored (may contain real secrets if used for personal overrides)
# Helm dependency tarballs — restored via `helm dependency update`
charts/
# Helm package output
*.tgz
# Rendered manifests (helm template output)
rendered/
manifests/
